// Score thresholds shared between client (display/hints) and server
// (the authoritative award decision) — see GAME_DESIGN.md §9.
export const RUSH_HOUR_UNLOCK_SCORE = 600;
export const MASTER_BAKER_SCORE = 800;
